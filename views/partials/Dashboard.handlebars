{{#if productos}}
    <div class="text-center mt-3"><h1>¡Bienvenido a Mercado Web!</h1></div>
    <div class="text-center"><h4>Por favor, seleccione sus productos</h4></div>
    
    <div class="container">
        <div class="row">
            {{#each productos}}
            <div class="col-sm-6 col-md-4 col-lg-3">
                {{> Producto
                producto=this
                }}
            </div>
            {{/each}}
        </div>
    </div>
{{/if}}

{{#unless productos}}
    <div class="alert alert-warning text-center mt-3" role="alert">
        No tenemos productos disponibles.
    </div>
{{/unless}}