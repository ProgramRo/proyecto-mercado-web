<div class="container">
    <div class="row">
        {{#each productos}}
        <div class="col-md-4">
            {{> Producto
            producto=this
            }}
        </div>
        {{/each}}
    </div>
</div>